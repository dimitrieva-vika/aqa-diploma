# Дипломный проект: Автоматизация тестирования веб-сервиса "Путешествие дня"

## Описание

Автотесты для веб-сервиса, предлагающего покупку тура двумя способами:
- Обычная оплата по дебетовой карте
- Выдача кредита по данным банковской карты

Сервис взаимодействует с двумя СУБД (MySQL и PostgreSQL) и эмулятором банковских сервисов (Gate Simulator).

## Технологии

- Java 11
- Gradle 8.14.5
- JUnit 5.10.2
- Selenide 6.19.1
- REST Assured 5.5.0
- Allure 2.24.0
- Docker & Docker Compose
- MySQL 8.0.41
- PostgreSQL 13
- Lombok 1.18.36
- JavaFaker 1.0.2

## Запуск тестов

### 1. Подготовка окружения

1. Убедитесь, что установлены:
    - Docker и Docker Compose
    - Java 11
    - Git

2. Клонируйте репозиторий:

```bash
git clone https://github.com/dimitrieva-vika/aqa-diploma.git
cd aqa-diploma
```

3. Запустите контейнеры с MySQL, PostgreSQL и эмулятором банковских сервисов:

```bash
docker-compose up -d
```

4. Дождитесь запуска всех контейнеров (около 30 секунд). Проверить статус можно командой:

```bash
docker ps
```

### 2. Запуск SUT

#### Для MySQL:

```bash
java -jar aqa-shop.jar -P:jdbc.url=jdbc:mysql://localhost:3306/app -P:jdbc.user=app -P:jdbc.password=pass
```

#### Для PostgreSQL:

```bash
java -jar aqa-shop.jar -P:jdbc.url=jdbc:postgresql://localhost:5432/app -P:jdbc.user=app -P:jdbc.password=pass
```

Приложение будет доступно по адресу: http://localhost:8080

### 3. Запуск тестов

```bash
./gradlew test --info
```

### 4. Генерация Allure отчета

```bash
./gradlew allureReport
```

### 5. Просмотр Allure отчета

```bash
./gradlew allureServe
```

Отчет автоматически откроется в браузере.

## Особенности тестов

- Тесты автоматически очищают БД перед каждым запуском
- Используются валидные и невалидные данные карт из эмулятора
- Проверяется корректность сохранения данных в БД
- Отчеты Allure с приложением скриншотов и Source Page

## Сценарии тестирования

Всего автоматизировано **26 сценариев**:

| Категория | Количество |
|-----------|------------|
| Позитивные сценарии (Debit) | 1 |
| Позитивные сценарии (Credit) | 1 |
| Негативные сценарии (Debit) | 1 |
| Негативные сценарии (Credit) | 1 |
| Валидация формы (Debit) | 8 |
| Валидация формы (Credit) | 8 |
| Проверки БД | 2 |
| Негативные сценарии (De) | 4 |
| **Итого** | **26** |

### Тестовые данные

| Тип | Номер карты | Статус |
|-----|-------------|--------|
| APPROVED | 4444 4444 4444 4441 | Успешный платеж |
| DECLINED | 4444 4444 4444 4442 | Отказ в платеже |

## Известные баги

| № | Описание | Серьезность | Ссылка |
|---|----------|-------------|--------|
| 1 | PostgreSQL: таблицы не создаются автоматически | Critical | [Issue #1] |
| 2 | При DECLINED отображается сообщение "Успешно! Операция одобрена Банком" | Major | [Issue #2] |
| 3 | При пустом CVC ошибка отображается у поля "Владелец" | Minor | [Issue #3] |

## Остановка окружения

Для остановки всех контейнеров:

```bash
docker-compose down
```

Для полной очистки данных БД:

```bash
docker-compose down -v
rm -rf data/
```

## Контакты

- GitHub: [dimitrieva-vika](https://github.com/dimitrieva-vika)
- Репозиторий: [aqa-diploma](https://github.com/dimitrieva-vika/aqa-diploma)